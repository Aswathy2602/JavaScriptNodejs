var colors={
    primary:'Blue',
    secondary:'Red',
    teritary:'White'
}

for(var color in colors){
    console.log(color+' -> '+colors[color])
}

var colours=["pink","orange",'yellow']

for(var col in colours){
    console.log(col+' -> '+colours[col])
}